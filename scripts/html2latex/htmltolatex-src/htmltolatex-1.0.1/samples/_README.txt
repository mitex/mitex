- files with .tex.bak extension come directly from html2latex
- files with .tex contain no LaTeX errors (at most 10 errors had
  to be corrected in each of these samples) and are formatted with
  AUCTeX extension of emacs